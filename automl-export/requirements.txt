# Install into a clean virtualenv (Python 3.11 recommended):
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# autogluon.tabular pulls in lightgbm, pandas, and numpy automatically.
# Developed/trained against autogluon 1.5.0 — pin if you hit compatibility issues.
autogluon.tabular[lightgbm]
scikit-learn
