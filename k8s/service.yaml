apiVersion: v1
kind: Service
metadata:
  name: crop-yield-agent
  namespace: team1
  labels:
    app.kubernetes.io/name: crop-yield-agent
    kagenti.io/type: agent
    protocol.kagenti.io/a2a: ""
spec:
  selector:
    app.kubernetes.io/name: crop-yield-agent
  ports:
    - name: http
      port: 8080
      targetPort: 8000
  type: ClusterIP