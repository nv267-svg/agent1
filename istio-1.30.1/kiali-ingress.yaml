apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: kiali-vs
  namespace: istio-system
spec:
  hosts:
  - "kiali.localtest.me"
  gateways:
  - kagenti-system/http
  http:
  - route:
    - destination:
        host: kiali
        port:
          number: 20001
